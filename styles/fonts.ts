import { Reenie_Beanie } from "next/font/google";
import { Inter } from "next/font/google";

export const HandwritingFont = Reenie_Beanie({
  subsets: ["latin"],
  weight: "400"
});

export const InterFont = Inter({ subsets: ["latin"] });
